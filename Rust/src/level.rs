pub struct LevelData {
    pub CONDITION: i32,
    pub IS_BLIND: i32,
    pub MIN: i32,
    pub MAX: i32,
    pub TOL: i32,
    pub W: i32,
    pub H: i32,
    pub TIME: i32,
    pub DENSE: i32,
    pub SIZE: i32,
    pub SPEED: i32,
    pub DIRECTION: i32,
}
